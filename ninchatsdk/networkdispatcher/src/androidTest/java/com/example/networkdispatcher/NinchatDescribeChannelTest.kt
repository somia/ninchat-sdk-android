package com.example.networkdispatcher

import org.junit.Test

import org.junit.Assert.*

// all test currently now should only delete guest user
// todo complete test
class NinchatDescribeChannelTest {
    @Test
    fun describeChannel_with_nullSession() {
        assertEquals(4, 2 + 2)
    }

    @Test
    fun describeChannel_with_noChannelId() {
        assertEquals(4, 2 + 2)
    }

    @Test
    fun describeChannel_with_wrongChannelId() {
        assertEquals(4, 2 + 2)
    }

    @Test
    fun describeChannel_with_right_sessionAndChannelId() {
        assertEquals(4, 2 + 2)
    }
}