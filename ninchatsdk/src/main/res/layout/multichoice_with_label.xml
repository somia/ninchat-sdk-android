<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical">

    <TextView
        android:id="@+id/multichoice_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/ninchat_color_info_text"
        tools:text="Element label"/>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/ninchat_chat_form_multichoice_options"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:orientation="vertical"
        tools:listitem="@layout/item_chat_multichoice_selected"
        tools:itemCount="3"/>
</LinearLayout>

